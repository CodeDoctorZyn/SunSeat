# Static Assets Directory

This directory is for static assets like CSS, JavaScript, and images.

Currently using CDN-based assets:
- Tailwind CSS (via CDN)
- HTMX (via CDN)  
- Leaflet (via CDN)

If you want to serve assets locally, place them here and update the base template.